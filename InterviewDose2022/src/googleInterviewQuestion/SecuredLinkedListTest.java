package googleInterviewQuestion;

public class SecuredLinkedListTest {
    /*@Test
    public void testAddValueAndIsValidChain() {
        SecuredLinkedList myList = new SecuredLinkedList();

        myList.addValue("A");
        myList.addValue("B");
        myList.addValue("C");

        assertTrue(myList.isValidChain());
    }

    @Test
    public void testHackAndIsValidChain() {
        SecuredLinkedList myList = new SecuredLinkedList();

        myList.addValue("A");
        myList.addValue("C");
        myList.hack("B", 1);

        assertTrue(myList.isValidChain());
    }

    @Test
    public void testInvalidChainValueChange() {
        SecuredLinkedList myList = new SecuredLinkedList();

        myList.addValue("A");
        myList.addValue("B");
        myList.addValue("C");

        // Modify a node's value, making it invalid
        myList.head.value = "X";

        assertFalse(myList.isValidChain());
    }

    @Test
    public void testInvalidChainHashChange() {
        SecuredLinkedList myList = new SecuredLinkedList();

        myList.addValue("A");
        myList.addValue("B");
        myList.addValue("C");

        // Modify a node's hashValue, making it invalid
        myList.head.hashValue = 12345;

        assertFalse(myList.isValidChain());
    }

    @Test
    public void testEmptyList() {
        SecuredLinkedList myList = new SecuredLinkedList();

        assertTrue(myList.isValidChain());
    }

    @Test
    public void testCycleDetection() {
        SecuredLinkedList myList = new SecuredLinkedList();

        myList.addValue("A");
        myList.addValue("B");
        myList.addValue("C");

        // Creating a cycle for testing (C points back to B)
        myList.head.next.next.next = myList.head.next;

        assertFalse(myList.isValidChain());
    }*/
}
